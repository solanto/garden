<header class="header">
    <span class="page-icon">{{ icon }}</span>
    <nav>
        {% for item in nav %}
        <a href="{{ item.link }}" {% if item.id %}id="{{ item.id }}"{% endif %}>{{ item.title }}</a>
        {% endfor %}
    </nav>
</header>
