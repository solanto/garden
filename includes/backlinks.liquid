{% if backlinks.length > 0 %}{{ backlinks.length }} incoming link{% unless backlinks.length == 1 %}s{% endunless %}:
<ul class="backlinks">
    {% for link in backlinks %}
    <li>
        <a href="{{ link.url }}">{{ link.title | downcase }}</a>
        <div class="preview" role="none">
            {{ link.preview | markdownify }}
        </div>
    </li>
    {% endfor %}
</ul>
{% else %}
no incoming links
{% endif %}
